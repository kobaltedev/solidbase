```tsx tab title="index.tsx" del={2,6} {10-12}
import {
	type RouteDefinition,
	createAsync,
} from "@solidjs/router";
import {
	type User,
	getUserQuery,
} from "./queries";

export const route = {
  preload: ({ params }) => getUserQuery({ id: params.id }),
} satisfies RouteDefinition;
```

```tsx tab title="queries.ts" del={3-5} {7-9}
import { query } from "@solidjs/router";

export type User = {
	id: number;
}

export const getUserQuery = query((id: number) => {
	return {id} as User
})
```
